spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:prospero
    username: sa
    password: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true

server:
  port: 8081
